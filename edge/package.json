{
  "name": "edge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "vitest run"
  },
  "dependencies": {
    "fastify": "^4.28.1",
    "dotenv": "^16.4.5",
    "@fastify/rate-limit": "^8.0.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "vitest": "^2.0.5",
    "tsx": "^4.19.2",
    "typescript": "^5.5.4"
  }
}
